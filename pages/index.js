import { Home as HomeView } from '../src/views'
export default HomeView
/*
  This seems redundant but allows for different layouts
  to wrap view as well as data fetching to be connected here
  And the view component is all contained the in the same folder
  which makes moving it to another file easier or switch out view
 */
